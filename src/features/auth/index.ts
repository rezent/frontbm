// Экспорт store
export { 
  auth, 
  user, 
  isAuthenticated, 
  isLoading, 
  error, 
  token, 
  isAdmin, 
  isUser 
} from './model/store';

// Экспорт actions
export { authActions } from './model/actions'; 